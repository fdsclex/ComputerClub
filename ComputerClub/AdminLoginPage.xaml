<Page x:Class="ComputerClub.AdminLoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:ComputerClub">

    <Grid>
        <StackPanel Width="340" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Авторизация администратора" 
                       FontSize="24" FontWeight="SemiBold" 
                       Foreground="{StaticResource AccentYellow}"
                       HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <TextBlock Text="Логин администратора" Style="{StaticResource FieldLabel}"/>
            <Grid>
                <TextBox x:Name="tbAdminLogin"
                         Style="{StaticResource DarkInput}"
                         Padding="12,10,120,10" 
                    TextChanged="TbAdminLogin_TextChanged"/>

                   
                    <TextBlock Text="@club.ru"
                           Style="{StaticResource FieldLabel}"
                           FontSize="15"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0,0,12,0"
                           IsHitTestVisible="False"/>
            </Grid>

            <TextBlock Text="Пароль" Style="{StaticResource FieldLabel}"/>
            <Grid>
                <PasswordBox x:Name="pbPassword"
                             Style="{StaticResource DarkInput}"
                             Padding="12,10,48,10"
                             Visibility="Visible"/>

                <TextBox x:Name="tbPasswordVisible"
                         Style="{StaticResource DarkInput}"
                         Padding="12,10,48,10"
                         Visibility="Collapsed"
                         IsReadOnly="False"/>

                <Button x:Name="btnTogglePassword"
                        Style="{StaticResource PasswordToggleButton}"
                        Click="TogglePassword_Click"/>
            </Grid>

            <Button Content="ВОЙТИ" 
                    Style="{StaticResource YellowActionButton}"
                    Margin="0,24,0,0"
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Page>