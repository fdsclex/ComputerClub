<Page x:Class="ComputerClub.ClientPanel.CabinetNotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Уведомления">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок + кнопка "Отметить все прочитанными" -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Уведомления"
                       FontSize="28" FontWeight="Bold"
                       Foreground="{StaticResource AccentYellow}"
                       VerticalAlignment="Center"/>
            <Button Content="Отметить все прочитанными"
                    Margin="30,0,0,0" Padding="16,8"
                    Style="{StaticResource YellowActionButton}"
                    Click="MarkAllRead_Click" />
        </StackPanel>

        <!-- Таблица уведомлений -->
        <DataGrid x:Name="dgNotifications" Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserSortColumns="True"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderInput}"
                  RowBackground="{StaticResource BgInput}"
                  AlternatingRowBackground="#161616"
                  BorderThickness="0"
                  RowHeaderWidth="0">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="MinHeight" Value="40"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRead}" Value="False">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Background" Value="#1E1E2E"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" 
                                    Binding="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" 
                                    Width="160"/>
                <DataGridTextColumn Header="Тип" 
                                    Binding="{Binding Type}" 
                                    Width="140"/>
                <DataGridTextColumn Header="Сообщение" 
                                    Binding="{Binding Message}" 
                                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Прочитано" Width="110"
                    Binding="{Binding IsRead}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRead}" Value="True">
                                    <Setter Property="Text" Value="✓ Да"/>
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsRead}" Value="False">
                                    <Setter Property="Text" Value="✗ Нет"/>
                                    <Setter Property="Foreground" Value="#F44336"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя информация -->
        <TextBlock x:Name="tbInfo" Grid.Row="2" Margin="0,16,0,0" FontSize="14"
                   Foreground="{StaticResource TextSecondary}"
                   Text="Загрузка уведомлений..."/>
    </Grid>
</Page>